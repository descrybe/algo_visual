import React from 'react';
import './Header.scss';

const Header: React.FC = () => {
    return (
        <header className='app-header'>
            <h1>ALGO VISUAL</h1>
        </header>
    );
};

export default Header;